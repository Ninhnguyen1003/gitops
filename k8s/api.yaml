apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: api
  namespace: demo
  labels:
    app: api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api
  template:
    metadata:
      labels:
        app: api
    spec:
      containers:
      - name: api
        image: w9-api:2 # Bắt buộc phải là số 2 để Minikube tìm thấy ảnh dưới máy local
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 8080
        env:
        - name: ERROR_RATE
          value: "0" # Bản v5 sạch lỗi hoàn toàn
        - name: VERSION
          value: "v5" # Định danh logic hiển thị trên hệ thống là v5
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8080
  strategy:
    canary:
      analysis:
        templates:
        - templateName: api-success-rate
        args:
        - name: service-name
          value: api
      steps:
      - setWeight: 25
      - pause: { duration: 20s }
      - setWeight: 50
      - pause: { duration: 20s }
      - setWeight: 100
---
apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: demo
  labels:
    app: api
spec:
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  selector:
    app: api